{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"reportnet-client","text":"<p>Python client for the EEA Reportnet 3 REST API.</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install reportnet\n\n# Optional: DataFrame support (polars, pandas, modin \u2014 via narwhals)\npip install \"reportnet[dataframe]\"\n\n# Optional: system keychain storage for API keys\npip install \"reportnet[keyring]\"\n</code></pre>"},{"location":"#quick-start","title":"Quick start","text":"<pre><code>from reportnet import ReportnetClient\n\nclient = ReportnetClient(api_key=\"your-api-key\")\n\n# Scope to a dataflow and a specific reporter country\nflow = client.for_dataflow(1619)\nie = flow.for_provider(42)   # Ireland's provider ID\n\n# Import a CSV\nie.import_file(dataset_id=93953, file=\"ireland.csv\").wait()\n\n# Export to DataFrames\nframes = ie.etl_export(dataset_id=93953).to_frames()\n</code></pre> <p>See Concepts for the full Reportnet data model, or jump straight to a guide.</p>"},{"location":"#secure-key-storage","title":"Secure key storage","text":"<p>Store API keys in the OS keychain (macOS Keychain, Windows Credential Manager, etc.) so they never appear in source code:</p> <pre><code>import reportnet\n\n# Save once\nreportnet.save_key(dataflow_id=1619, api_key=\"your-api-key\")\n\n# Load at runtime\nclient = ReportnetClient.from_keyring(dataflow_id=1619)\n</code></pre>"},{"location":"concepts/","title":"Concepts","text":""},{"location":"concepts/#the-reportnet-hierarchy","title":"The Reportnet hierarchy","text":"<p>Reportnet 3 organises data in a three-level hierarchy:</p> <pre><code>Dataflow\n  \u2514\u2500\u2500 Reporter / DataProvider  (a country or organisation)\n        \u2514\u2500\u2500 Dataset  (the data store for that reporter)\n              \u2514\u2500\u2500 Table\n                    \u2514\u2500\u2500 Field (column)\n  \u2514\u2500\u2500 Reference Dataset  (shared lookup data, no reporter)\n</code></pre>"},{"location":"concepts/#dataflow","title":"Dataflow","text":"<p>A Dataflow represents a reporting obligation \u2014 for example, the \"EU Greenhouse Gas Inventory\" or \"Air Quality e-Reporting\". It defines:</p> <ul> <li>what tables and fields are expected (the schema)</li> <li>which countries or organisations must report (the reporters)</li> <li>deadlines and release schedules</li> </ul> <p>Each dataflow has a numeric ID visible in the Reportnet URL.</p>"},{"location":"concepts/#reporter-dataprovider","title":"Reporter / DataProvider","text":"<p>A Reporter (also called a DataProvider) is a country or organisation that submits data to a dataflow. Each reporter has a numeric <code>provider_id</code>. You can look up provider IDs by country code using the provider helpers.</p>"},{"location":"concepts/#dataset","title":"Dataset","text":"<p>Each reporter has one Dataset per dataflow. This is where their actual data rows live. A dataset contains one or more tables, each matching the schema defined by the dataflow.</p>"},{"location":"concepts/#reference-dataset","title":"Reference Dataset","text":"<p>A Reference Dataset is a special dataset shared across all reporters in a dataflow. It typically contains codelists and lookup values (e.g. gas types, notation keys). Reference datasets are managed by the dataflow custodian and have no <code>provider_id</code>.</p>"},{"location":"concepts/#client-hierarchy","title":"Client hierarchy","text":"<p>The library mirrors this structure:</p> <pre><code># Top-level client \u2014 one per API key\nclient = ReportnetClient(api_key=\"...\")\n\n# Dataflow-scoped client \u2014 pre-fills dataflow_id\nflow = client.for_dataflow(1619)\nflow.get_dataflow()    # DataflowInfo: name, type, status\nflow.get_reporters()   # list of Reporter objects with provider_id and dataset_id\n\n# Reporter-scoped client \u2014 pre-fills dataflow_id + provider_id\nie = flow.for_provider(42)\nie.import_file(dataset_id=93953, file=\"data.csv\")\n</code></pre> <p><code>for_provider()</code> returns a new <code>DataflowClient</code> with the <code>provider_id</code> set. All import, export, and validation calls on it automatically include that ID.</p>"},{"location":"concepts/#sync-vs-async-operations","title":"Sync vs async operations","text":"<p>Some API operations complete synchronously (schema lookup, validation results). Most data operations \u2014 imports, exports, validation jobs \u2014 are asynchronous: the API accepts the request and returns a job ID, then processes it in the background.</p> <p>The library wraps these in a <code>JobHandle</code>:</p> <pre><code>handle = ie.import_file(dataset_id=93953, file=\"data.csv\")\n# handle is a JobHandle \u2014 the import is running in the background\n\nhandle.wait()  # blocks until done; raises JobFailedError on failure\n</code></pre>"},{"location":"concepts/#bigdata-vs-citus","title":"BigData vs Citus","text":"<p>Reportnet has two storage backends:</p> Backend Description Endpoints Citus Standard PostgreSQL-based storage <code>etl_export</code>, <code>etl_import</code>, <code>list_group_validations</code> BigData (DLT2) Large-scale columnar storage <code>export_file_dl</code>, <code>export_dataset_file_dl</code>, <code>list_group_validations_dl</code> <p>Use <code>flow.is_big_dataflow()</code> to check which backend a dataflow uses and pick the right methods.</p>"},{"location":"notebooks/","title":"Interactive Notebooks","text":"<p>Three marimo notebooks are included for hands-on exploration. Each opens in your browser and connects to the Reportnet API using a key stored in your system keychain.</p> <p>All notebooks have a Sandbox checkbox to connect to <code>sandbox.reportnet.europa.eu</code> instead of production \u2014 useful for testing without affecting live data.</p>"},{"location":"notebooks/#run-locally","title":"Run locally","text":"<pre><code>uv sync --group explore\nuv run marimo edit notebooks/01_explore_dataflow.py\n</code></pre> <p>For the spatial notebook also install geopandas:</p> <pre><code>pip install \"reportnet-client[spatial]\"\nuv run marimo edit notebooks/03_spatial_geodataframe.py\n</code></pre>"},{"location":"notebooks/#static-previews","title":"Static previews","text":"<p>The links below are non-interactive snapshots exported on the last push to <code>main</code>. They show the notebook layout and documentation but cannot make live API calls.</p> Notebook Preview Purpose 01 \u2014 Explore a Dataflow open Browse reporters, inspect schemas, visualise the dataflow structure, download empty templates 02 \u2014 Import / Export Pipeline open End-to-end import \u2192 validate \u2192 export workflow 03 \u2014 Spatial GeoDataFrame open Export spatial datasets and convert to a geopandas GeoDataFrame"},{"location":"api/client/","title":"ReportnetClient","text":"<p>The top-level client. One instance per API key. Obtain a <code>DataflowClient</code> via <code>for_dataflow()</code> to avoid repeating <code>dataflow_id</code> on every call.</p>"},{"location":"api/client/#reportnet.ReportnetClient","title":"ReportnetClient","text":""},{"location":"api/client/#reportnet.ReportnetClient.from_keyring","title":"from_keyring  <code>classmethod</code>","text":"<pre><code>from_keyring(\n    dataflow_id: int | str,\n    base_url: str | None = None,\n    *,\n    sandbox: bool = False,\n    timeout: float = 30.0,\n) -&gt; \"ReportnetClient\"\n</code></pre> <p>Create a client using the API key stored in the system keychain.</p> <p>Parameters:</p> Name Type Description Default <code>dataflow_id</code> <code>int | str</code> <p>The dataflow ID the key was stored under.</p> required <code>base_url</code> <code>str | None</code> <p>API base URL.  Defaults to :data:<code>SANDBOX_URL</code> when <code>sandbox=True</code>, otherwise :data:<code>PRODUCTION_URL</code>.</p> <code>None</code> <code>sandbox</code> <code>bool</code> <p>If <code>True</code>, use the sandbox environment and look up the sandbox key (stored separately from the production key).</p> <code>False</code> <code>timeout</code> <code>float</code> <p>HTTP request timeout in seconds.</p> <code>30.0</code> <p>Examples::</p> <pre><code># Production (default)\nclient = ReportnetClient.from_keyring(1619)\n\n# Sandbox\nclient = ReportnetClient.from_keyring(1619, sandbox=True)\n</code></pre>"},{"location":"api/client/#reportnet.ReportnetClient.for_dataflow","title":"for_dataflow","text":"<pre><code>for_dataflow(\n    dataflow_id: int, *, provider_id: int | None = None\n) -&gt; \"DataflowClient\"\n</code></pre> <p>Return a DataflowClient that pre-fills dataflow_id (and optionally provider_id).</p>"},{"location":"api/client/#reportnet.ReportnetClient.ping","title":"ping","text":"<pre><code>ping(*, dataflow_id: int) -&gt; bool\n</code></pre> <p>Return True if the API key is valid and the API is reachable.</p> <p>Makes a single lightweight GET request.  Returns False on auth failure; raises on network errors (so transient connectivity issues surface as exceptions rather than a silent False).</p> <p>Example::</p> <pre><code>if not client.ping(dataflow_id=1619):\n    raise RuntimeError(\"API key is invalid or has been revoked\")\n</code></pre>"},{"location":"api/client/#reportnet.ReportnetClient.get_dataflow","title":"get_dataflow","text":"<pre><code>get_dataflow(*, dataflow_id: int) -&gt; DataflowInfo\n</code></pre> <p>GET /dataflow/v1/{dataflowId} \u2014 name, type, status of a dataflow.</p>"},{"location":"api/client/#reportnet.ReportnetClient.get_reporters","title":"get_reporters","text":"<pre><code>get_reporters(*, dataflow_id: int) -&gt; list[Reporter]\n</code></pre> <p>GET /representative/v1/dataflow/{dataflowId} \u2014 countries/orgs reporting to a dataflow.</p>"},{"location":"api/client/#reportnet.ReportnetClient.get_reporting_datasets","title":"get_reporting_datasets","text":"<pre><code>get_reporting_datasets(\n    *, dataflow_id: int\n) -&gt; list[ReportingDataset]\n</code></pre> <p>GET /dataflow/v1/{dataflowId} \u2014 all reporting datasets (one per provider \u00d7 table).</p> <p>Each country (reporter) has one dataset per table schema defined in the dataflow. Filter by <code>provider_id</code> to get all datasets for a specific country.</p>"},{"location":"api/client/#reportnet.ReportnetClient.get_reference_datasets","title":"get_reference_datasets","text":"<pre><code>get_reference_datasets(\n    *, dataflow_id: int\n) -&gt; list[ReferenceDataset]\n</code></pre> <p>GET /dataflow/v1/{dataflowId} \u2014 all reference datasets for a dataflow.</p> <p>Reference datasets hold shared lookup data such as codelists. They are not tied to any specific reporter.</p>"},{"location":"api/client/#reportnet.ReportnetClient.get_test_datasets","title":"get_test_datasets","text":"<pre><code>get_test_datasets(*, dataflow_id: int) -&gt; list[TestDataset]\n</code></pre> <p>GET /dataflow/v1/{dataflowId} \u2014 all test datasets for a dataflow.</p> <p>Test datasets mirror the reporting schema and are used by custodians to verify validation rules before the reporting period opens.</p>"},{"location":"api/client/#reportnet.ReportnetClient.is_big_dataflow","title":"is_big_dataflow","text":"<pre><code>is_big_dataflow(*, dataflow_id: int) -&gt; bool\n</code></pre> <p>Return True if dataflow_id is a BigData (DLT2) dataflow.</p> <p>Reads the <code>bigData</code> field from GET /dataflow/v1/{dataflowId}. The dedicated GET /dataflow/private/v1/{dataflowId}/isBigDataflow endpoint looks purpose-built for this but 404s for API-key auth regardless of the dataflow's actual BigData status, so it isn't used here.</p>"},{"location":"api/client/#reportnet.ReportnetClient.import_file","title":"import_file","text":"<pre><code>import_file(\n    *,\n    dataset_id: int,\n    dataflow_id: int,\n    file: Union[str, Path, bytes, IO[bytes], object],\n    filename: str | None = None,\n    provider_id: int | None = None,\n    table_schema_id: str | None = None,\n    replace: bool = False,\n    delimiter: str = \"|\",\n    integration_id: int | None = None,\n) -&gt; JobHandle\n</code></pre> <p>POST /dataset/v2/importFileData/{datasetId} \u2014 multipart upload.</p>"},{"location":"api/client/#reportnet.ReportnetClient.etl_import","title":"etl_import","text":"<pre><code>etl_import(\n    *,\n    dataset_id: int,\n    dataflow_id: int,\n    tables: list[dict[str, object]],\n    replace_data: bool = False,\n) -&gt; JobHandle\n</code></pre> <p>POST /dataset/v1/{datasetId}/etlImport \u2014 JSON body, Citus datasets only.</p>"},{"location":"api/client/#reportnet.ReportnetClient.etl_export","title":"etl_export","text":"<pre><code>etl_export(\n    *,\n    dataset_id: int,\n    dataflow_id: int,\n    provider_id: int | None = None,\n    data_provider_codes: str | None = None,\n    table_schema_id: str | None = None,\n    include_attachments: bool = False,\n    version: int = 4,\n) -&gt; JobHandle\n</code></pre> <p>GET /dataset/v{version}/etlExport/{datasetId} \u2014 async export.</p> <p>v4 (BigData/DLT2, recommended): result is a ZIP of CSVs. v5 (analytics, opt-in): result is a ZIP of Parquet files \u2014 same shape as v4, smaller and faster to load; never selected automatically, pass <code>version=5</code> explicitly. v3 (Citus): result is JSON; requires <code>data_provider_codes</code> (ISO country code).</p>"},{"location":"api/client/#reportnet.ReportnetClient.export_file","title":"export_file","text":"<pre><code>export_file(\n    *,\n    dataset_id: int,\n    table_schema_id: str,\n    mime_type: Literal[\"csv\", \"xlsx\"] = \"csv\",\n    filters: dict[str, object] | None = None,\n) -&gt; JobHandle\n</code></pre> <p>POST /dataset/exportFile \u2014 async single-table export.</p>"},{"location":"api/client/#reportnet.ReportnetClient.export_file_dl","title":"export_file_dl","text":"<pre><code>export_file_dl(\n    *,\n    dataset_id: int,\n    table_schema_id: str,\n    filters: dict[str, object] | None = None,\n) -&gt; JobHandle\n</code></pre> <p>POST /dataset/exportFileDL \u2014 async single-table export, BigData variant.</p>"},{"location":"api/client/#reportnet.ReportnetClient.export_dataset_file","title":"export_dataset_file","text":"<pre><code>export_dataset_file(\n    *,\n    dataset_id: int,\n    mime_type: Literal[\"csv\", \"xlsx\", \"zip\"] = \"csv\",\n) -&gt; JobHandle\n</code></pre> <p>GET /dataset/exportDatasetFile \u2014 async whole-dataset export (all tables).</p>"},{"location":"api/client/#reportnet.ReportnetClient.export_dataset_file_dl","title":"export_dataset_file_dl","text":"<pre><code>export_dataset_file_dl(*, dataset_id: int) -&gt; JobHandle\n</code></pre> <p>GET /dataset/exportDatasetFileDL \u2014 async whole-dataset export, BigData variant.</p>"},{"location":"api/client/#reportnet.ReportnetClient.add_validation_job","title":"add_validation_job","text":"<pre><code>add_validation_job(\n    *,\n    dataset_id: int,\n    dataflow_id: int,\n    provider_id: int | None = None,\n) -&gt; JobHandle\n</code></pre> <p>PUT /orchestrator/jobs/addValidationJob/{datasetId}.</p>"},{"location":"api/client/#reportnet.ReportnetClient.list_group_validations","title":"list_group_validations","text":"<pre><code>list_group_validations(\n    *,\n    dataset_id: int,\n    dataflow_id: int,\n    provider_id: int | None = None,\n) -&gt; dict[str, object]\n</code></pre> <p>GET /validation/listGroupValidations/{datasetId} \u2014 Citus datasets.</p>"},{"location":"api/client/#reportnet.ReportnetClient.list_group_validations_dl","title":"list_group_validations_dl","text":"<pre><code>list_group_validations_dl(\n    *,\n    dataset_id: int,\n    dataflow_id: int,\n    provider_id: int | None = None,\n) -&gt; dict[str, object]\n</code></pre> <p>GET /validation/listGroupValidationsDL/{datasetId} \u2014 BigData datasets.</p>"},{"location":"api/client/#reportnet.ReportnetClient.download_validation_snapshot","title":"download_validation_snapshot","text":"<pre><code>download_validation_snapshot(\n    *,\n    snapshot_id: int,\n    dataset_id: int,\n    dataflow_id: int,\n    provider_id: int,\n) -&gt; bytes\n</code></pre> <p>GET /downloadValidation/{snapshotId} \u2014 validation results for a release snapshot.</p>"},{"location":"api/client/#reportnet.ReportnetClient.get_schema","title":"get_schema","text":"<pre><code>get_schema(*, dataset_id: int) -&gt; DatasetSchema\n</code></pre> <p>GET /dataschema/v1/datasetId/{datasetId} \u2014 table and field definitions.</p>"},{"location":"api/client/#reportnet.ReportnetClient.set_reference_dataset_updatable","title":"set_reference_dataset_updatable","text":"<pre><code>set_reference_dataset_updatable(\n    *, dataset_id: int, dataflow_id: int, updatable: bool\n) -&gt; None\n</code></pre> <p>PUT /referenceDataset/{datasetId} \u2014 lock or unlock a reference dataset.</p>"},{"location":"api/client/#reportnet.ReportnetClient.delete_dataset_data","title":"delete_dataset_data","text":"<pre><code>delete_dataset_data(\n    *,\n    dataset_id: int,\n    dataflow_id: int,\n    provider_id: int | None = None,\n    delete_prefilled_tables: bool = False,\n) -&gt; None\n</code></pre> <p>DELETE /dataset/v1/{datasetId}/deleteDatasetData \u2014 remove all data from a dataset.</p>"},{"location":"api/client/#reportnet.ReportnetClient.delete_table_data","title":"delete_table_data","text":"<pre><code>delete_table_data(\n    *,\n    dataset_id: int,\n    table_schema_id: str,\n    dataflow_id: int,\n    provider_id: int | None = None,\n) -&gt; None\n</code></pre> <p>DELETE /dataset/v1/{datasetId}/deleteTableData/{tableSchemaId}.</p>"},{"location":"api/client/#reportnet.ReportnetClient.list_historic_releases","title":"list_historic_releases","text":"<pre><code>list_historic_releases(\n    *, dataset_id: int, dataflow_id: int\n) -&gt; list[dict[str, object]]\n</code></pre> <p>GET /snapshot/v1/historicReleases \u2014 list releases submitted for a dataset.</p>"},{"location":"api/client/#reportnet.ReportnetClient.check_import_process","title":"check_import_process","text":"<pre><code>check_import_process(\n    *, dataset_id: int\n) -&gt; dict[str, object]\n</code></pre> <p>GET /dataset/checkImportProcess/{datasetId} \u2014 lock/import status for a dataset.</p>"},{"location":"api/client/#connect_interactive","title":"connect_interactive","text":"<p>A one-call, non-raising connect helper for interactive tools (used by the example notebooks) \u2014 returns <code>(flow, error_message)</code> instead of raising, so callers can render the error directly in a UI.</p>"},{"location":"api/client/#reportnet.connect_interactive","title":"connect_interactive","text":"<pre><code>connect_interactive(\n    dataflow_id: int,\n    *,\n    sandbox: bool = False,\n    country_code: str | None = None,\n) -&gt; tuple[DataflowClient | None, str | None]\n</code></pre> <p>Connect for interactive/notebook use, returning a UI-ready result.</p> <p>Loads the API key from the system keychain and builds a :class:<code>DataflowClient</code> scoped to dataflow_id. When country_code is given, further scopes it to that reporter via :meth:<code>DataflowClient.find_reporter</code>. When country_code is omitted, validates the key with a single :meth:<code>DataflowClient.ping</code> call instead (there's no specific reporter to resolve).</p> <p>This exists mainly to back the \"Connect\" cell shared by the example marimo notebooks under <code>notebooks/</code> \u2014 see there for usage \u2014 but is generally useful for any interactive tool that wants a one-call, non-raising connect step.</p> <p>Parameters:</p> Name Type Description Default <code>dataflow_id</code> <code>int</code> <p>The dataflow to connect to.</p> required <code>sandbox</code> <code>bool</code> <p>Use the sandbox environment and sandbox key.</p> <code>False</code> <code>country_code</code> <code>str | None</code> <p>ISO 3166-1 alpha-2 code to scope to a specific reporter.</p> <code>None</code> <p>Returns:</p> Type Description <code>DataflowClient | None</code> <p><code>(flow, error_message)</code>. On success, <code>error_message</code> is</p> <code>str | None</code> <p><code>None</code>. On failure, <code>flow</code> is <code>None</code> and <code>error_message</code> is</p> <code>tuple[DataflowClient | None, str | None]</code> <p>a short, human-readable string safe to show directly in a UI</p> <code>tuple[DataflowClient | None, str | None]</code> <p>callout.</p> <p>Example::</p> <pre><code>flow, error = reportnet.connect_interactive(1619, country_code=\"IE\")\nif error:\n    mo.callout(mo.md(error), kind=\"danger\")\nelse:\n    mo.callout(mo.md(f\"Connected \u2014 provider_id={flow._provider_id}\"), kind=\"success\")\n</code></pre>"},{"location":"api/dataflow-client/","title":"DataflowClient","text":"<p>A <code>DataflowClient</code> is scoped to a single dataflow and optionally a single reporter. Obtain one via <code>ReportnetClient.for_dataflow()</code>.</p> <pre><code>flow = client.for_dataflow(1619)          # dataflow scope only\nie = flow.for_provider(42)               # further scoped to reporter 42\n</code></pre> <p>All methods automatically fill in <code>dataflow_id</code> (and <code>provider_id</code> when set).</p>"},{"location":"api/dataflow-client/#reportnet.DataflowClient","title":"DataflowClient","text":"<p>Convenience wrapper around ReportnetClient scoped to a single dataflow.</p> <p>Obtain via <code>client.for_dataflow(dataflow_id, provider_id=...)</code>.</p> <p>The Reportnet hierarchy is::</p> <pre><code>Dataflow\n  \u2514\u2500\u2500 Reporter / DataProvider (country or organisation)\n        \u2514\u2500\u2500 Dataset (reporting dataset, one per reporter)\n  \u2514\u2500\u2500 Reference Datasets (shared; no provider_id)\n</code></pre> <p>Usage::</p> <pre><code># Custodian / no specific reporter\ndf = client.for_dataflow(1619)\ndf.get_dataflow()          # dataflow metadata\ndf.get_reporters()         # list of countries and their dataset IDs\n\n# Scoped to a specific reporter country\nie = df.for_provider(42)\nie.import_file(dataset_id=93953, file=\"data.csv\")\nie.add_validation_job(dataset_id=93953)\nframes = ie.etl_export(dataset_id=93953).to_frames()\n\n# Reference datasets (custodian only, no provider_id)\ndf.import_file(dataset_id=REF_DS_ID, file=\"codelists.csv\")\ndf.set_reference_dataset_updatable(dataset_id=REF_DS_ID, updatable=False)\n</code></pre>"},{"location":"api/dataflow-client/#reportnet.DataflowClient.for_provider","title":"for_provider","text":"<pre><code>for_provider(provider_id: int) -&gt; 'DataflowClient'\n</code></pre> <p>Return a new DataflowClient scoped to a specific reporter / country.</p> <p>Example::</p> <pre><code>df = client.for_dataflow(1619)\nie = df.for_provider(42)   # Ireland's provider ID\nie.import_file(dataset_id=93953, file=\"ireland.csv\")\n</code></pre>"},{"location":"api/dataflow-client/#reportnet.DataflowClient.find_reporter","title":"find_reporter","text":"<pre><code>find_reporter(country_code: str) -&gt; 'DataflowClient'\n</code></pre> <p>Return a DataflowClient scoped to a reporter identified by ISO country code.</p> <p>Looks up the reporter in the dataflow and returns a scoped client in one step \u2014 no need to know the numeric <code>provider_id</code> in advance.</p> <p>Raises :class:<code>ValueError</code> if no reporter matches the country code, or if multiple reporters are registered for the same code (use :meth:<code>get_reporters</code> to inspect them and call :meth:<code>for_provider</code> with the correct <code>provider_id</code>).</p> <p>Parameters:</p> Name Type Description Default <code>country_code</code> <code>str</code> <p>ISO 3166-1 alpha-2 code, e.g. <code>\"IE\"</code>, <code>\"DE\"</code>.</p> required <p>Returns:</p> Name Type Description <code>A</code> <code>'DataflowClient'</code> <p>class:<code>DataflowClient</code> scoped to that reporter's <code>provider_id</code>.</p> <p>Example::</p> <pre><code>ie = flow.find_reporter(\"IE\")\ndatasets = ie.get_reporting_datasets()\n# [ReportingDataset(id=93953, table_name='Table1a', ...)]\nie.import_file(dataset_id=datasets[0].id, file=\"ireland.csv\")\n</code></pre>"},{"location":"api/dataflow-client/#reportnet.DataflowClient.ping","title":"ping","text":"<pre><code>ping() -&gt; bool\n</code></pre> <p>Return True if the API key is valid and the API is reachable.</p>"},{"location":"api/dataflow-client/#reportnet.DataflowClient.get_dataflow","title":"get_dataflow","text":"<pre><code>get_dataflow() -&gt; DataflowInfo\n</code></pre> <p>Return name, type and status of this dataflow.</p>"},{"location":"api/dataflow-client/#reportnet.DataflowClient.get_reporters","title":"get_reporters","text":"<pre><code>get_reporters() -&gt; list[Reporter]\n</code></pre> <p>Return the list of countries/organisations registered for this dataflow.</p>"},{"location":"api/dataflow-client/#reportnet.DataflowClient.get_reporting_datasets","title":"get_reporting_datasets","text":"<pre><code>get_reporting_datasets() -&gt; list[ReportingDataset]\n</code></pre> <p>Return reporting datasets for this dataflow.</p> <p>When the client is scoped to a provider (via :meth:<code>for_provider</code>), returns only that provider's datasets.  Otherwise returns all reporters' datasets (one per reporter \u00d7 table schema).</p> <p>Example::</p> <pre><code># Scoped \u2014 returns only Ireland's datasets\nie = flow.for_provider(42)\ndatasets = ie.get_reporting_datasets()\n# [ReportingDataset(id=93953, table_name='Table1a', ...),\n#  ReportingDataset(id=93954, table_name='Table7', ...)]\n\n# Unscoped \u2014 returns every reporter's datasets\nall_ds = flow.get_reporting_datasets()\n</code></pre>"},{"location":"api/dataflow-client/#reportnet.DataflowClient.get_reference_datasets","title":"get_reference_datasets","text":"<pre><code>get_reference_datasets() -&gt; list[ReferenceDataset]\n</code></pre> <p>Return all reference datasets for this dataflow.</p> <p>Reference datasets hold shared lookup data such as codelists and are not tied to any specific reporter.  Use the returned <code>id</code> values as <code>ref_dataset_id</code> when calling :meth:<code>get_codelists</code>.</p> <p>Example::</p> <pre><code>ref_ds = flow.get_reference_datasets()\n# [ReferenceDataset(id=93975, name='Reference Dataset - Codelist', ...)]\ncodelists = flow.get_codelists(dataset_id=93953, ref_dataset_id=ref_ds[0].id)\n</code></pre>"},{"location":"api/dataflow-client/#reportnet.DataflowClient.get_test_datasets","title":"get_test_datasets","text":"<pre><code>get_test_datasets() -&gt; list[TestDataset]\n</code></pre> <p>Return all test datasets for this dataflow.</p> <p>Test datasets mirror the reporting schema and can be used by custodians to verify validation rules before the reporting period opens.</p> <p>Example::</p> <pre><code>test_ds = flow.get_test_datasets()\n# [TestDataset(id=93953, name='Test Dataset - Table1a', ...)]\nflow.import_file(dataset_id=test_ds[0].id, file=\"sample.csv\")\n</code></pre>"},{"location":"api/dataflow-client/#reportnet.DataflowClient.is_big_dataflow","title":"is_big_dataflow","text":"<pre><code>is_big_dataflow() -&gt; bool\n</code></pre> <p>Return True if this is a BigData (DLT2) dataflow.</p> <p>Cached per instance \u2014 BigData-ness doesn't change over the lifetime of a DataflowClient, and this is now consulted by more than one method (etl_export, import_file).</p>"},{"location":"api/dataflow-client/#reportnet.DataflowClient.import_file","title":"import_file","text":"<pre><code>import_file(\n    *,\n    dataset_id: int,\n    file: Union[str, Path, bytes, IO[bytes], object],\n    filename: str | None = None,\n    provider_id: int | None = None,\n    table_schema_id: str | None = None,\n    replace: bool = False,\n    delimiter: str = \"|\",\n    integration_id: int | None = None,\n) -&gt; JobHandle\n</code></pre> <p>POST /dataset/v2/importFileData/{datasetId} \u2014 multipart upload.</p> <p>Unlike other methods on this class, <code>provider_id</code> is not auto-filled from the stored <code>provider_id</code> for BigData (DLT2) dataflows \u2014 the API 403s if <code>providerId</code> is present at all, even the correct one. See :meth:<code>etl_export</code> for the same behavior on the export side.</p>"},{"location":"api/dataflow-client/#reportnet.DataflowClient.import_frames","title":"import_frames","text":"<pre><code>import_frames(\n    *,\n    dataset_id: int,\n    frames: dict[str, object],\n    replace: bool = False,\n    delimiter: str = \"|\",\n    poll_interval: float = 5.0,\n    timeout: float | None = None,\n) -&gt; None\n</code></pre> <p>Import multiple tables from a dict of DataFrames (or DuckDB relations).</p> <p>Keys in frames must match table names in the dataset schema. Each table is uploaded and polled to completion sequentially.</p> <p>Also accepts DuckDB relations \u2014 they are converted to polars DataFrames automatically before serialisation.</p> <p>Parameters:</p> Name Type Description Default <code>dataset_id</code> <code>int</code> <p>The dataset to import into.</p> required <code>frames</code> <code>dict[str, object]</code> <p>Mapping of table name \u2192 DataFrame (polars, pandas, modin) or DuckDB relation.</p> required <code>replace</code> <code>bool</code> <p>If True, replace all existing rows before importing each table.</p> <code>False</code> <code>delimiter</code> <code>str</code> <p>CSV column separator (default <code>|</code>).</p> <code>'|'</code> <code>poll_interval</code> <code>float</code> <p>Seconds between job status polls.</p> <code>5.0</code> <code>timeout</code> <code>float | None</code> <p>Maximum seconds to wait per table import.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If a key in frames does not match any table in the schema.</p> <p>Example::</p> <pre><code>import polars as pl\nframes = {\n    \"Table1a\": pl.DataFrame({\"category\": [...], \"cyear\": [...]}),\n    \"Table7\":  pl.DataFrame({...}),\n}\nflow.import_frames(dataset_id=93953, frames=frames)\n\n# Also works with a DuckDB relation\nimport duckdb\ncon = duckdb.connect()\nrel = con.sql(\"SELECT * FROM 'my_data.parquet'\")\nflow.import_frames(dataset_id=93953, frames={\"Table1a\": rel})\n</code></pre>"},{"location":"api/dataflow-client/#reportnet.DataflowClient.etl_export","title":"etl_export","text":"<pre><code>etl_export(\n    *,\n    dataset_id: int,\n    provider_id: int | None = None,\n    data_provider_codes: str | None = None,\n    table_schema_id: str | None = None,\n    include_attachments: bool = False,\n    version: int | None = None,\n) -&gt; JobHandle\n</code></pre> <p>Export a dataset via the ETL endpoint.</p> <p>version selects the API version:</p> <ul> <li><code>4</code> (default for BigData/DLT2 dataflows) \u2014 asynchronous, returns a ZIP of CSVs</li> <li><code>3</code> (default for Citus dataflows) \u2014 asynchronous, returns JSON;   requires a country code (<code>dataProviderCodes</code>), injected automatically   when the client was created via :meth:<code>find_reporter</code>.</li> <li><code>5</code> (analytics, opt-in) \u2014 asynchronous, returns a ZIP of Parquet files;   same shape as v4 but smaller/faster to load. Must be requested   explicitly with <code>version=5</code>; never chosen automatically.</li> </ul> <p>When version is <code>None</code> (the default), the correct version is chosen automatically by calling :meth:<code>is_big_dataflow</code> \u2014 between v3 and v4 only.</p> <p>Parameters:</p> Name Type Description Default <code>data_provider_codes</code> <code>str | None</code> <p>ISO 3166-1 alpha-2 country code passed as <code>dataProviderCodes</code> to the v3 endpoint (e.g. <code>\"FR\"</code>). Inferred automatically when the client was obtained via :meth:<code>find_reporter</code>.</p> <code>None</code> <code>provider_id</code> <code>int | None</code> <p>Unlike other methods on this class, this is not auto-filled from the <code>provider_id</code> this client was scoped with (e.g. via :meth:<code>for_provider</code> / :meth:<code>find_reporter</code>). For v4/v5 (BigData), <code>dataset_id</code> already identifies a single provider's dataset, and sending <code>providerId</code> \u2014 even the correct one \u2014 gets a 403 from the API for reporter-level keys. Only pass this if you have confirmed your key/dataflow combination needs it.</p> <code>None</code>"},{"location":"api/dataflow-client/#reportnet.DataflowClient.validate","title":"validate","text":"<pre><code>validate(\n    *,\n    dataset_id: int,\n    provider_id: int | None = None,\n    poll_interval: float = 10.0,\n    timeout: float | None = None,\n    on_status: Callable[[JobStatus], None] | None = None,\n) -&gt; ValidationResult\n</code></pre> <p>Trigger validation, wait for it to finish, and return structured results.</p> <p>Combines :meth:<code>add_validation_job</code> + :meth:<code>~reportnet.JobHandle.wait</code> + :meth:<code>list_group_validations_dl</code> in one call.</p> <p>Parameters:</p> Name Type Description Default <code>dataset_id</code> <code>int</code> <p>Dataset to validate.</p> required <code>provider_id</code> <code>int | None</code> <p>Override the stored <code>provider_id</code> for this call.</p> <code>None</code> <code>poll_interval</code> <code>float</code> <p>Seconds between job status polls.</p> <code>10.0</code> <code>timeout</code> <code>float | None</code> <p>Maximum seconds to wait for the validation job.</p> <code>None</code> <code>on_status</code> <code>Callable[[JobStatus], None] | None</code> <p>Optional callback called with each :class:<code>~reportnet.JobStatus</code> during polling \u2014 useful for progress updates.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>A</code> <code>ValidationResult</code> <p>class:<code>~reportnet.ValidationResult</code> with parsed issues and a</p> <code>ValidationResult</code> <p><code>raw</code> attribute containing the full API response.</p> <p>Raises:</p> Type Description <p>class:<code>~reportnet.DatasetLockedError</code>: If another job is already running.</p> <p>class:<code>~reportnet.JobFailedError</code>: If the validation job fails.</p> <p>class:<code>~reportnet.JobTimeoutError</code>: If timeout is exceeded.</p> <p>Example::</p> <pre><code>result = flow.validate(\n    dataset_id=93953,\n    on_status=lambda s: print(f\"  {s}\"),\n)\nif result.has_blockers:\n    print(\"Blockers found \u2014 cannot submit:\")\n    print(result.to_frame())\nelse:\n    print(result.summary())\n</code></pre>"},{"location":"api/dataflow-client/#reportnet.DataflowClient.get_codelists","title":"get_codelists","text":"<pre><code>get_codelists(\n    *,\n    dataset_id: int,\n    ref_dataset_id: int,\n    poll_interval: float = 5.0,\n    timeout: float | None = None,\n) -&gt; dict[str, list[str]]\n</code></pre> <p>Return valid values for all LINK fields in dataset_id.</p> <p>Exports the reference dataset (ref_dataset_id), then maps each LINK field in the reporting dataset to the sorted list of valid values from the column it references.</p> <p>Parameters:</p> Name Type Description Default <code>dataset_id</code> <code>int</code> <p>The reporting dataset whose LINK fields to resolve.</p> required <code>ref_dataset_id</code> <code>int</code> <p>The reference dataset that holds the codelist data.</p> required <code>poll_interval</code> <code>float</code> <p>Seconds between export polling calls.</p> <code>5.0</code> <code>timeout</code> <code>float | None</code> <p>Maximum seconds to wait for the export job.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, list[str]]</code> <p>A dict mapping field name \u2192 list of valid string values.</p> <p>Example::</p> <pre><code>codelists = df.get_codelists(dataset_id=93953, ref_dataset_id=12345)\n# {\"category\": [\"Total excluding LULUCF\", \"Total including LULUCF\"],\n#  \"scenario\": [\"WAM\", \"WEM\", \"WOM\"], \"ry\": [\"0\", \"1\"]}\n\n# Use when building a template DataFrame \u2014 LINK columns become Enum\ntemplate = (\n    df.get_schema(dataset_id=93953).table(\"Table1a\").to_frame(codelists=codelists)\n)\n</code></pre>"},{"location":"api/dataflow-client/#reportnet.DataflowClient.get_template","title":"get_template","text":"<pre><code>get_template(\n    *,\n    dataset_id: int,\n    ref_dataset_id: int | None = None,\n    poll_interval: float = 5.0,\n    timeout: float | None = None,\n) -&gt; \"dict[str, object]\"\n</code></pre> <p>Return empty, fully-typed DataFrames for every table in dataset_id.</p> <p>Combines schema introspection and codelist resolution in one call:</p> <ol> <li>Fetches the dataset schema (field names and types).</li> <li>Locates the reference dataset \u2014 uses ref_dataset_id if given,    otherwise picks the first reference dataset for this dataflow    automatically.  If the dataflow has no reference datasets, LINK /    CODELIST columns are left as plain strings.</li> <li>Exports the reference data and resolves codelist values.</li> <li> <p>Returns one empty DataFrame per table, with:</p> </li> <li> <p>Numeric / date / boolean columns cast to their schema types.</p> </li> <li>LINK and CODELIST columns cast to <code>pl.Enum</code> (polars) or      <code>CategoricalDtype</code> (pandas) so invalid values are rejected      at assignment time rather than silently accepted.</li> </ol> <p>Requires <code>pip install reportnet[dataframe]</code>.</p> <p>Parameters:</p> Name Type Description Default <code>dataset_id</code> <code>int</code> <p>The reporting dataset to build templates for.</p> required <code>ref_dataset_id</code> <code>int | None</code> <p>ID of the reference dataset to pull codelists from. Auto-detected from the dataflow when omitted.</p> <code>None</code> <code>poll_interval</code> <code>float</code> <p>Seconds between polls while exporting the reference data.</p> <code>5.0</code> <code>timeout</code> <code>float | None</code> <p>Maximum seconds to wait for the reference export job.</p> <code>None</code> <p>Returns:</p> Type Description <code>'dict[str, object]'</code> <p>A <code>dict</code> mapping table name \u2192 empty typed DataFrame.</p> <p>Example::</p> <pre><code>templates = flow.get_template(dataset_id=93953)\n# {\"Table1a\": &lt;empty polars.DataFrame with Enum columns&gt;,\n#  \"Table7\":  &lt;empty polars.DataFrame&gt;}\n\n# Fill a table and upload\nimport polars as pl\ndf = pl.concat([templates[\"Table1a\"], pl.DataFrame({\n    \"category\": [\"Total including LULUCF\"],\n    \"cyear\":    [2024],\n    \"cvalue\":   [1234.5],\n}).cast({col: templates[\"Table1a\"].schema[col]\n         for col in [\"category\"]})])\nflow.import_file(dataset_id=93953, file=df)\n</code></pre>"},{"location":"api/dataflow-client/#reportnet.DataflowClient.delete_dataset_data","title":"delete_dataset_data","text":"<pre><code>delete_dataset_data(\n    *,\n    dataset_id: int,\n    provider_id: int | None = None,\n    delete_prefilled_tables: bool = False,\n) -&gt; None\n</code></pre> <p>Remove all data from a dataset (use before a full replace import).</p>"},{"location":"api/dataflow-client/#reportnet.DataflowClient.delete_table_data","title":"delete_table_data","text":"<pre><code>delete_table_data(\n    *,\n    dataset_id: int,\n    table_schema_id: str,\n    provider_id: int | None = None,\n) -&gt; None\n</code></pre> <p>Remove all data from a single table within a dataset.</p>"},{"location":"api/dataflow-client/#reportnet.DataflowClient.list_historic_releases","title":"list_historic_releases","text":"<pre><code>list_historic_releases(\n    *, dataset_id: int\n) -&gt; list[dict[str, object]]\n</code></pre> <p>Return all releases (submissions) made for a dataset.</p>"},{"location":"api/dataflow-client/#reportnet.DataflowClient.check_import_process","title":"check_import_process","text":"<pre><code>check_import_process(\n    *, dataset_id: int\n) -&gt; dict[str, object]\n</code></pre> <p>Return lock/import status for a dataset.</p>"},{"location":"api/dataflow-client/#reportnet.DataflowClient.set_reference_dataset_updatable","title":"set_reference_dataset_updatable","text":"<pre><code>set_reference_dataset_updatable(\n    *, dataset_id: int, updatable: bool\n) -&gt; None\n</code></pre> <p>Lock (updatable=False) or unlock (updatable=True) a reference dataset.</p>"},{"location":"api/dataflow-client/#reportnet.DataflowClient.to_mermaid","title":"to_mermaid","text":"<pre><code>to_mermaid(*, include_test: bool = False) -&gt; str\n</code></pre> <p>Return a Mermaid diagram string describing this dataflow's structure.</p> <p>Renders natively in marimo without any CLI tools::</p> <pre><code>mo.mermaid(flow.to_mermaid())\n</code></pre> <p>One compact node per reporter country, coloured by their worst submission status across all tables (green = all FINAL, yellow = correction requested, grey = pending).  Reference and test datasets are shown as separate nodes connected to the dataflow.</p> <p>Parameters:</p> Name Type Description Default <code>include_test</code> <code>bool</code> <p>When True, also show test datasets (one extra API call).</p> <code>False</code> <p>Returns:</p> Type Description <code>str</code> <p>A Mermaid <code>graph LR</code> diagram string.</p>"},{"location":"api/exceptions/","title":"Exceptions","text":"<p>All exceptions inherit from <code>ReportnetError</code>.</p> <pre><code>ReportnetError\n  APIError(status_code, response_body)\n    AuthError          # 401 / 403\n    DatasetLockedError # 423 \u2014 another job is already running\n    RateLimitError     # 429\n  JobFailedError(job_id, status)   # job ended in a non-FINISHED terminal state\n  JobTimeoutError(job_id)          # wait() exceeded its timeout\n</code></pre>"},{"location":"api/exceptions/#reportnet.ReportnetError","title":"ReportnetError","text":"<p>               Bases: <code>Exception</code></p>"},{"location":"api/exceptions/#reportnet.APIError","title":"APIError","text":"<p>               Bases: <code>ReportnetError</code></p>"},{"location":"api/exceptions/#reportnet.AuthError","title":"AuthError","text":"<p>               Bases: <code>APIError</code></p>"},{"location":"api/exceptions/#reportnet.DatasetLockedError","title":"DatasetLockedError","text":"<p>               Bases: <code>APIError</code></p> <p>Raised on HTTP 423 Locked \u2014 another job is already running on this dataset.</p>"},{"location":"api/exceptions/#reportnet.RateLimitError","title":"RateLimitError","text":"<p>               Bases: <code>APIError</code></p> <p>Raised on HTTP 429 Too Many Requests.</p>"},{"location":"api/exceptions/#reportnet.JobFailedError","title":"JobFailedError","text":"<p>               Bases: <code>ReportnetError</code></p>"},{"location":"api/exceptions/#reportnet.JobTimeoutError","title":"JobTimeoutError","text":"<p>               Bases: <code>ReportnetError</code></p>"},{"location":"api/keychain/","title":"Key storage","text":"<p>Helpers for storing and retrieving API keys in the OS keychain (macOS Keychain, Windows Credential Manager, libsecret on Linux).</p> <p>Requires <code>pip install \"reportnet[keyring]\"</code>.</p>"},{"location":"api/keychain/#reportnet.save_key","title":"save_key","text":"<pre><code>save_key(\n    dataflow_id: int | str,\n    api_key: str,\n    *,\n    sandbox: bool = False,\n) -&gt; None\n</code></pre> <p>Store an API key for a dataflow in the system keychain.</p> <p>Pass <code>sandbox=True</code> to store a sandbox key separately from the production key.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If api_key is empty or whitespace-only \u2014 storing a blank key would silently corrupt every future request made with it.</p>"},{"location":"api/keychain/#reportnet.get_key","title":"get_key","text":"<pre><code>get_key(\n    dataflow_id: int | str, *, sandbox: bool = False\n) -&gt; str\n</code></pre> <p>Retrieve an API key for a dataflow from the system keychain.</p> <p>Pass <code>sandbox=True</code> to retrieve the sandbox key.</p> <p>A blank (empty or whitespace-only) stored value is treated the same as \"not found\" \u2014 it can only have gotten there via a caller that bypassed :func:<code>save_key</code>'s validation, and silently using it would produce a malformed <code>Authorization</code> header deep in the HTTP layer instead of a clear error here.</p>"},{"location":"api/keychain/#reportnet.delete_key","title":"delete_key","text":"<pre><code>delete_key(\n    dataflow_id: int | str, *, sandbox: bool = False\n) -&gt; None\n</code></pre> <p>Remove an API key for a dataflow from the system keychain.</p> <p>Pass <code>sandbox=True</code> to delete the sandbox key.</p>"},{"location":"api/models/","title":"Models","text":""},{"location":"api/models/#job-handling","title":"Job handling","text":""},{"location":"api/models/#reportnet.JobHandle","title":"JobHandle  <code>dataclass</code>","text":""},{"location":"api/models/#reportnet.JobHandle.to_frames","title":"to_frames","text":"<pre><code>to_frames(\n    *,\n    poll_interval: float = 5.0,\n    timeout: float | None = None,\n    on_status: Callable[[JobStatus], None] | None = None,\n) -&gt; dict[str, Any]\n</code></pre> <p>Wait for an export job and return its tables as DataFrames.</p> <p>Supports CSV (v4), Parquet (v5), and JSON (v3) export formats \u2014 see :func:<code>~reportnet._util.zip_to_frames</code>.</p> <p>Returns a dict keyed by table name (filename without extension). Requires polars or pandas (<code>pip install reportnet[dataframe]</code>).</p>"},{"location":"api/models/#reportnet.JobStatus","title":"JobStatus","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p>"},{"location":"api/models/#dataflow","title":"Dataflow","text":""},{"location":"api/models/#reportnet.DataflowInfo","title":"DataflowInfo  <code>dataclass</code>","text":"<p>Metadata returned by GET /dataflow/v1/{dataflowId}.</p>"},{"location":"api/models/#reportnet.Reporter","title":"Reporter  <code>dataclass</code>","text":"<p>A country/organisation registered to report within a dataflow.</p> <p>Returned by GET /representative/v1/dataflow/{dataflowId}. Use :meth:<code>DataflowClient.get_reporting_datasets</code> to find the actual dataset IDs for each reporter (one dataset per table schema).</p>"},{"location":"api/models/#reportnet.Reporter.country_code","title":"country_code  <code>property</code>","text":"<pre><code>country_code: str | None\n</code></pre> <p>ISO 3166-1 alpha-2 country code, or None if provider_id is not in the mapping.</p>"},{"location":"api/models/#reportnet.Reporter.country_name","title":"country_name  <code>property</code>","text":"<pre><code>country_name: str | None\n</code></pre> <p>Full country name, or None if provider_id is not in the mapping.</p>"},{"location":"api/models/#dataset-schema","title":"Dataset schema","text":""},{"location":"api/models/#reportnet.DatasetSchema","title":"DatasetSchema  <code>dataclass</code>","text":""},{"location":"api/models/#reportnet.DatasetSchema.table","title":"table","text":"<pre><code>table(name: str) -&gt; TableSchema\n</code></pre> <p>Return the TableSchema with the given name, or raise KeyError.</p>"},{"location":"api/models/#reportnet.DatasetSchema.to_frames","title":"to_frames","text":"<pre><code>to_frames(\n    *, codelists: dict[str, list[str]] | None = None\n) -&gt; dict[str, NativeFrame]\n</code></pre> <p>Return a dict of empty DataFrames, one per table, keyed by table name.</p> <p>Parameters:</p> Name Type Description Default <code>codelists</code> <code>dict[str, list[str]] | None</code> <p>Optional mapping of field name \u2192 valid values (from :meth:<code>DataflowClient.get_codelists</code>).  When provided, LINK columns use <code>pl.Enum</code> / <code>CategoricalDtype</code> instead of strings.</p> <code>None</code> <p>Requires <code>pip install reportnet[dataframe]</code>.</p> <p>Example::</p> <pre><code>schema = client.get_schema(dataset_id=93953)\nframes = schema.to_frames()\n# {\"Table1a\": &lt;empty DataFrame&gt;, \"Table1b\": &lt;empty DataFrame&gt;}\nprint(frames[\"Table1a\"].dtypes)\n</code></pre>"},{"location":"api/models/#reportnet.TableSchema","title":"TableSchema  <code>dataclass</code>","text":""},{"location":"api/models/#reportnet.TableSchema.required_columns","title":"required_columns","text":"<pre><code>required_columns() -&gt; list[str]\n</code></pre> <p>Names of fields that are required for import.</p>"},{"location":"api/models/#reportnet.TableSchema.column_names","title":"column_names","text":"<pre><code>column_names() -&gt; list[str]\n</code></pre> <p>All field names in schema order.</p>"},{"location":"api/models/#reportnet.TableSchema.validate_frame","title":"validate_frame","text":"<pre><code>validate_frame(\n    frame: object,\n    *,\n    codelists: dict[str, list[str]] | None = None,\n) -&gt; list[str]\n</code></pre> <p>Check a DataFrame against this table schema before uploading.</p> <p>Returns a list of error strings; an empty list means the frame is safe to upload.  Checks:</p> <ul> <li>All required columns are present.</li> <li>CODELIST / LINK column values exist in codelists (when provided).</li> </ul> <p>Does not raise; callers decide how to handle errors.</p> <p>Parameters:</p> Name Type Description Default <code>frame</code> <code>object</code> <p>A polars or pandas DataFrame.</p> required <code>codelists</code> <code>dict[str, list[str]] | None</code> <p>Optional mapping returned by :meth:<code>DataflowClient.get_codelists</code>.</p> <code>None</code> <p>Example::</p> <pre><code>errors = schema.table(\"Table1a\").validate_frame(df, codelists=codelists)\nif errors:\n    raise ValueError(\"\\n\".join(errors))\n</code></pre>"},{"location":"api/models/#reportnet.TableSchema.cast_frame","title":"cast_frame","text":"<pre><code>cast_frame(\n    frame: object,\n    *,\n    codelists: dict[str, list[str]] | None = None,\n) -&gt; NativeFrame\n</code></pre> <p>Cast a DataFrame's columns to this table's schema types.</p> <p>Use this when reading data from Excel or CSV where columns arrive as the wrong type (e.g., integers as floats, dates as strings).  LINK / CODELIST columns are cast to <code>Enum</code> when codelists is provided, which rejects invalid values at cast time rather than silently uploading bad data.</p> <p>Raises :class:<code>ValueError</code> if required columns are missing or if any Enum cast fails.</p> <p>Parameters:</p> Name Type Description Default <code>frame</code> <code>object</code> <p>A polars or pandas DataFrame.</p> required <code>codelists</code> <code>dict[str, list[str]] | None</code> <p>Optional mapping returned by :meth:<code>DataflowClient.get_codelists</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>NativeFrame</code> <p>The cast DataFrame in the same backend as the input.</p> <p>Example::</p> <pre><code>import polars as pl\n\nraw = pl.read_excel(\"my_data.xlsx\", sheet_name=\"Table1a\")\ntyped = schema.table(\"Table1a\").cast_frame(raw, codelists=codelists)\nflow.import_file(dataset_id=..., file=typed)\n</code></pre>"},{"location":"api/models/#reportnet.TableSchema.to_frame","title":"to_frame","text":"<pre><code>to_frame(\n    *, codelists: dict[str, list[str]] | None = None\n) -&gt; NativeFrame\n</code></pre> <p>Return an empty DataFrame with columns and types matching this table.</p> <p>Useful for building import data with the correct schema, or for inspecting what the API expects before uploading.</p> <p>Parameters:</p> Name Type Description Default <code>codelists</code> <code>dict[str, list[str]] | None</code> <p>Optional mapping of field name \u2192 valid values (from :meth:<code>DataflowClient.get_codelists</code>).  When provided, LINK columns use <code>pl.Enum</code> (polars) or <code>CategoricalDtype</code> (pandas) instead of plain strings.</p> <code>None</code> <p>Requires <code>pip install reportnet[dataframe]</code>. Returns a <code>polars.DataFrame</code> if polars is installed, else <code>pandas.DataFrame</code>.</p> <p>Example::</p> <pre><code>codelists = flow.get_codelists(dataset_id=93953, ref_dataset_id=12345)\nframe = schema.table(\"Table1a\").to_frame(codelists=codelists)\n# LINK columns are now pl.Enum with the valid categories\n</code></pre>"},{"location":"api/models/#reportnet.FieldSchema","title":"FieldSchema  <code>dataclass</code>","text":""},{"location":"api/models/#reportnet.FieldType","title":"FieldType","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p>"},{"location":"api/providers/","title":"Provider helpers","text":""},{"location":"api/providers/#reportnet.DataProvider","title":"DataProvider","text":"<p>               Bases: <code>NamedTuple</code></p>"},{"location":"api/providers/#reportnet.by_id","title":"by_id","text":"<pre><code>by_id(provider_id: int) -&gt; DataProvider | None\n</code></pre> <p>Return the DataProvider for a given provider ID, or None if not found.</p>"},{"location":"api/providers/#reportnet.by_country","title":"by_country","text":"<pre><code>by_country(country_code: str) -&gt; list[DataProvider]\n</code></pre> <p>Return all DataProviders for a given ISO country code (e.g. 'AT', 'IE').</p>"},{"location":"api/providers/#reportnet.by_group","title":"by_group","text":"<pre><code>by_group(\n    group: str, *, field: GroupField = \"eea_group\"\n) -&gt; list[DataProvider]\n</code></pre> <p>Return all DataProviders belonging to a named group.</p> <p>group: e.g. 'EEA', 'EU', 'EFTA', 'ENP East', 'EEA Cooperating country', 'Other'. field: 'eea_group' (default) or 'eurostat_group'.</p>"},{"location":"guides/export/","title":"Export data","text":"<p>All export methods return a <code>JobHandle</code>. The job runs in the background; call <code>.result()</code> for raw bytes or <code>.to_frames()</code> to get DataFrames directly.</p>"},{"location":"guides/export/#etl-export-zip-of-csvs-recommended","title":"ETL export \u2014 ZIP of CSVs (recommended)","text":"<p>Returns a ZIP archive with one CSV per table.</p> <pre><code>ie = client.for_dataflow(1619).for_provider(42)\n\nhandle = ie.etl_export(dataset_id=93953)\nzip_bytes = handle.result(poll_interval=10.0, timeout=600.0)\n\nwith open(\"export.zip\", \"wb\") as f:\n    f.write(zip_bytes)\n</code></pre>"},{"location":"guides/export/#export-directly-to-dataframes","title":"Export directly to DataFrames","text":"<p>Requires <code>pip install \"reportnet[dataframe]\"</code>.</p> <pre><code>frames = ie.etl_export(dataset_id=93953).to_frames(poll_interval=10.0, timeout=600.0)\n# {\"Table1a\": &lt;polars.DataFrame&gt;, \"Table1b\": &lt;polars.DataFrame&gt;}\n\nfor name, frame in frames.items():\n    print(name, frame.shape)\n    print(frame.head())\n</code></pre> <p><code>to_frames()</code> tries polars first, falls back to pandas.</p>"},{"location":"guides/export/#progress-callback","title":"Progress callback","text":"<pre><code>frames = ie.etl_export(dataset_id=93953).to_frames(\n    poll_interval=10.0,\n    timeout=600.0,\n    on_status=lambda s: print(f\"export: {s}\"),\n)\n</code></pre>"},{"location":"guides/export/#etl-export-v5-parquet","title":"ETL export v5 \u2014 Parquet","text":"<p>Use <code>version=5</code> to receive Parquet files instead of CSVs \u2014 same shape as v4, smaller and faster to load. It's opt-in only and never chosen automatically. <code>to_frames()</code> handles it transparently, same as v4:</p> <pre><code>frames = ie.etl_export(dataset_id=93953, version=5).to_frames(poll_interval=10.0, timeout=600.0)\n# {\"Table1a\": &lt;polars.DataFrame&gt;, \"Table1b\": &lt;polars.DataFrame&gt;}\n</code></pre> <p>Reading Parquet with the pandas backend additionally requires <code>pyarrow</code> or <code>fastparquet</code>; polars reads Parquet natively with no extra dependency.</p>"},{"location":"guides/export/#single-table-export","title":"Single-table export","text":"<pre><code># Standard (Citus)\nhandle = ie.export_file(\n    dataset_id=93953,\n    table_schema_id=\"68dd41f0...\",\n    mime_type=\"xlsx\",   # \"csv\" or \"xlsx\"\n)\nxlsx_bytes = handle.result()\n\n# BigData variant\nhandle = ie.export_file_dl(\n    dataset_id=93953,\n    table_schema_id=\"68dd41f0...\",\n)\n</code></pre>"},{"location":"guides/export/#whole-dataset-export","title":"Whole-dataset export","text":"<pre><code># All tables in one file (CSV, XLSX or ZIP of CSVs)\nhandle = ie.export_dataset_file(dataset_id=93953, mime_type=\"zip\")\nzip_bytes = handle.result()\n\n# BigData variant\nhandle = ie.export_dataset_file_dl(dataset_id=93953)\n</code></pre>"},{"location":"guides/export/#choosing-the-right-export-method","title":"Choosing the right export method","text":"Situation Method Standard (Citus) dataset, all tables <code>etl_export()</code> BigData dataset, all tables <code>etl_export()</code> (v4/v5) One table, CSV/XLSX (Citus) <code>export_file()</code> One table, BigData <code>export_file_dl()</code> Whole dataset, user-facing format <code>export_dataset_file()</code>"},{"location":"guides/import/","title":"Import data","text":""},{"location":"guides/import/#basic-import","title":"Basic import","text":"<pre><code>flow = client.for_dataflow(1619)\nie = flow.for_provider(42)   # reporter's provider_id\n\nhandle = ie.import_file(\n    dataset_id=93953,\n    file=\"data.csv\",\n)\nhandle.wait()\n</code></pre> <p><code>.wait()</code> blocks until the import job finishes. It raises <code>JobFailedError</code> if the job ends in any status other than <code>FINISHED</code>.</p>"},{"location":"guides/import/#replace-vs-append","title":"Replace vs append","text":"<pre><code># Append rows (default)\nie.import_file(dataset_id=93953, file=\"new_rows.csv\", replace=False)\n\n# Replace all existing data\nie.import_file(dataset_id=93953, file=\"full_dataset.csv\", replace=True)\n\n# Or: delete first, then import\nie.delete_dataset_data(dataset_id=93953)\nie.import_file(dataset_id=93953, file=\"full_dataset.csv\")\n</code></pre>"},{"location":"guides/import/#target-a-specific-table","title":"Target a specific table","text":"<pre><code>ie.import_file(\n    dataset_id=93953,\n    file=\"table1a.csv\",\n    table_schema_id=\"68dd41f045f9450001260da7\",  # from the Reportnet URL\n)\n</code></pre>"},{"location":"guides/import/#file-formats-accepted","title":"File formats accepted","text":"Type Example File path <code>\"data.csv\"</code> or <code>Path(\"data.csv\")</code> Raw bytes <code>b\"col1\\|col2\\n1\\|2\"</code> File object <code>open(\"data.csv\", \"rb\")</code> DataFrame <code>polars.DataFrame(...)</code> or <code>pandas.DataFrame(...)</code>"},{"location":"guides/import/#delimiter","title":"Delimiter","text":"<p>The Reportnet API expects pipe (<code>|</code>) as the default delimiter, not commas. This is the default in the library. Override it if needed:</p> <pre><code>ie.import_file(dataset_id=93953, file=\"data.csv\", delimiter=\",\")\n</code></pre>"},{"location":"guides/import/#importing-from-a-dataframe","title":"Importing from a DataFrame","text":"<pre><code>import polars as pl\n\ndata = pl.DataFrame({\n    \"category\": [\"Total including LULUCF\"],\n    \"cyear\": [2024],\n    \"gas\": [\"CO2\"],\n    \"cvalue\": [1234.5],\n})\n\nie.import_file(\n    dataset_id=93953,\n    file=data,\n    table_schema_id=\"68dd41f045f9450001260da7\",\n)\n</code></pre> <p>!!! note     The <code>record_id</code> column must not be included \u2014 Reportnet assigns it on     ingestion. Use <code>get_schema()</code> to see which columns are expected.</p>"},{"location":"guides/import/#progress-callback","title":"Progress callback","text":"<pre><code>handle = ie.import_file(dataset_id=93953, file=\"data.csv\")\nhandle.wait(\n    poll_interval=5.0,\n    timeout=300.0,\n    on_status=lambda s: print(f\"import: {s}\"),\n)\n</code></pre>"},{"location":"guides/import/#etl-import-citus-json","title":"ETL import (Citus / JSON)","text":"<p>For Citus datasets only, you can push structured JSON directly:</p> <pre><code>ie.etl_import(\n    dataset_id=93953,\n    tables=[{\n        \"tableSchemaId\": \"68dd41f0...\",\n        \"records\": [\n            {\"fields\": [{\"fieldSchemaId\": \"abc\", \"value\": \"Total\"}]},\n        ],\n    }],\n).wait()\n</code></pre>"},{"location":"guides/providers/","title":"Provider helpers","text":"<p>Every reporter in Reportnet has a numeric <code>provider_id</code>. The library ships a built-in lookup table so you can find IDs without hitting the API.</p>"},{"location":"guides/providers/#look-up-by-id","title":"Look up by ID","text":"<pre><code>from reportnet import by_id\n\np = by_id(42)\n# DataProvider(provider_id=42, country_code='AD', name='Andorra', eea_group='Other', eurostat_group='Other')\n</code></pre>"},{"location":"guides/providers/#look-up-by-country-code","title":"Look up by country code","text":"<pre><code>from reportnet import by_country\n\nproviders = by_country(\"IE\")\n# [DataProvider(provider_id=..., country_code='IE', name='Ireland', ...)]\n</code></pre> <p>A country may appear more than once (e.g. if it has both a primary and an additional entry in Reportnet). Check <code>provider_id</code> against the value in <code>get_reporters()</code> to find the right one for a specific dataflow.</p>"},{"location":"guides/providers/#filter-by-group","title":"Filter by group","text":"<pre><code>from reportnet import by_group\n\n# All EEA member countries\neea = by_group(\"EEA\")\n\n# EU member states (using Eurostat classification)\neu = by_group(\"EU\", field=\"eurostat_group\")\n\n# EEA cooperating countries\ncoop = by_group(\"EEA Cooperating country\")\n</code></pre> <p><code>field</code> defaults to <code>\"eea_group\"</code>. Use <code>\"eurostat_group\"</code> for Eurostat-style groupings.</p>"},{"location":"guides/providers/#all-providers","title":"All providers","text":"<pre><code>from reportnet import PROVIDERS\n\nfor p in PROVIDERS:\n    print(p.provider_id, p.country_code, p.name)\n</code></pre>"},{"location":"guides/providers/#get-reporters-for-a-dataflow","title":"Get reporters for a dataflow","text":"<p>For the authoritative list of which providers are active in a specific dataflow (including their assigned dataset IDs), use the API:</p> <pre><code>reporters = client.for_dataflow(1619).get_reporters()\nfor r in reporters:\n    print(r.provider_id, r.dataset_id)\n</code></pre>"},{"location":"guides/reference-datasets/","title":"Reference datasets","text":"<p>Reference datasets hold shared lookup and codelist data for a dataflow \u2014 things like gas types, notation keys, or country codes. They are:</p> <ul> <li>managed by the dataflow custodian (no <code>provider_id</code>)</li> <li>shared across all reporters in the dataflow</li> <li>used as the source for <code>LINK</code> and <code>CODELIST</code> field types</li> </ul>"},{"location":"guides/reference-datasets/#import-reference-data","title":"Import reference data","text":"<pre><code>flow = client.for_dataflow(1619)   # custodian \u2014 no provider scope\n\nflow.import_file(\n    dataset_id=REF_DATASET_ID,\n    file=\"codelists.csv\",\n    replace=True,\n)\n</code></pre>"},{"location":"guides/reference-datasets/#lock-and-unlock","title":"Lock and unlock","text":"<p>Lock the reference dataset to prevent edits during active reporting:</p> <pre><code># Lock (reporters can read but not write)\nflow.set_reference_dataset_updatable(dataset_id=REF_DATASET_ID, updatable=False)\n\n# Unlock (allow custodian edits)\nflow.set_reference_dataset_updatable(dataset_id=REF_DATASET_ID, updatable=True)\n</code></pre>"},{"location":"guides/reference-datasets/#export-reference-data","title":"Export reference data","text":"<pre><code>frames = flow.etl_export(dataset_id=REF_DATASET_ID).to_frames()\n</code></pre>"},{"location":"guides/reference-datasets/#resolve-link-field-codelists","title":"Resolve LINK field codelists","text":"<p>Reporting datasets use <code>LINK</code> fields to reference values from a reference dataset. <code>get_codelists()</code> exports the reference dataset and maps each LINK field in the reporting dataset to its sorted list of valid values:</p> <pre><code>codelists = flow.get_codelists(dataset_id=93953, ref_dataset_id=REF_DATASET_ID)\n# {\"category\": [\"Total excluding LULUCF\", \"Total including LULUCF\"],\n#  \"scenario\": [\"WAM\", \"WEM\", \"WOM\"],\n#  \"ry\": [\"0\", \"1\"]}\n</code></pre> <p>Pass the result to <code>to_frame()</code> to get LINK columns as <code>pl.Enum</code> (polars) or <code>CategoricalDtype</code> (pandas), which enforces valid values at assignment time:</p> <pre><code>schema = flow.get_schema(dataset_id=93953)\ntemplate = schema.table(\"Table1a\").to_frame(codelists=codelists)\n</code></pre> <p>See the Schema guide for more detail.</p>"},{"location":"guides/reference-datasets/#finding-the-reference-dataset-id","title":"Finding the reference dataset ID","text":"<p>The dataset ID appears in the Reportnet URL when you navigate to the reference dataset tab in the UI:</p> <pre><code>https://reportnet.europa.eu/dataflow/1619/dataset/12345?tab=...\n                                                     ^^^^^\n                                               REF_DATASET_ID\n</code></pre>"},{"location":"guides/schema/","title":"Dataset schema","text":"<p>The <code>get_schema()</code> method returns the full table and field definitions for a dataset. Use it to discover column names, types, and which fields are required before building an import CSV.</p>"},{"location":"guides/schema/#retrieve-a-schema","title":"Retrieve a schema","text":"<pre><code>flow = client.for_dataflow(1619)\n\nschema = flow.get_schema(dataset_id=93953)\n# DatasetSchema(id=\"...\", name=\"...\", tables=(...))\n</code></pre>"},{"location":"guides/schema/#explore-tables-and-fields","title":"Explore tables and fields","text":"<pre><code>for table in schema.tables:\n    print(f\"\\n{table.name}\")\n    print(f\"  required: {table.required_columns()}\")\n    print(f\"  all:      {table.column_names()}\")\n</code></pre> <p>Example output:</p> <pre><code>Table1a\n  required: ['category', 'scenario', 'ry', 'cyear', 'gas']\n  all:      ['category', 'scenario', 'ry', 'cyear', 'gas', 'cvalue', 'notation', 'inventorySubmissionYear']\n</code></pre>"},{"location":"guides/schema/#look-up-a-specific-table","title":"Look up a specific table","text":"<pre><code>table = schema.table(\"Table1a\")   # raises KeyError if not found\n\nfor field in table.fields:\n    print(field.name, field.type, \"required\" if field.required else \"\")\n</code></pre> <pre><code>category   FieldType.LINK            required\nscenario   FieldType.LINK            required\nry         FieldType.LINK            required\ncyear      FieldType.NUMBER_INTEGER  required\ngas        FieldType.LINK            required\ncvalue     FieldType.NUMBER_DECIMAL\nnotation   FieldType.LINK\n</code></pre>"},{"location":"guides/schema/#fieldtype-values","title":"FieldType values","text":"Value Meaning <code>TEXT</code> Free text <code>NUMBER_INTEGER</code> Whole number <code>NUMBER_DECIMAL</code> Decimal number <code>DATE</code> Date (ISO 8601) <code>DATETIME</code> Date + time <code>BOOLEAN</code> True / False <code>CODELIST</code> Single value from a fixed list <code>MULTISELECT_CODELIST</code> Multiple values from a fixed list <code>LINK</code> Reference to a value in a reference dataset <code>MULTISELECT_LINK</code> Multiple references <code>ATTACHMENT</code> File attachment <p>Unknown types pass through as opaque strings so the client doesn't break when the API adds new types.</p>"},{"location":"guides/schema/#link-fields-resolving-valid-values","title":"LINK fields \u2014 resolving valid values","text":"<p><code>LINK</code> fields store references to a column in a reference dataset. Each such field exposes the source metadata:</p> <pre><code>for field in table.fields:\n    if field.referenced_schema_id:\n        print(\n            f\"{field.name} \u2192 \"\n            f\"schema {field.referenced_schema_id}, \"\n            f\"pk field {field.referenced_pk_id}\"\n        )\n# category \u2192 schema 68dd410245f9450001260d45, pk field 68dd418645f9450001260d6e\n# scenario \u2192 schema 68dd410245f9450001260d45, pk field 68dd419a45f9450001260d7a\n</code></pre> <p>Use <code>get_codelists()</code> to export the reference dataset and resolve the valid values in one call. The result maps field name \u2192 sorted list of valid strings:</p> <pre><code>codelists = flow.get_codelists(dataset_id=93953, ref_dataset_id=REF_DATASET_ID)\n# {\"category\": [\"Total excluding LULUCF\", \"Total including LULUCF\"],\n#  \"scenario\": [\"WAM\", \"WEM\", \"WOM\"],\n#  \"ry\": [\"0\", \"1\"]}\n</code></pre> <p>Pass <code>codelists</code> to <code>to_frame()</code> so LINK columns become <code>pl.Enum</code> (polars) or <code>CategoricalDtype</code> (pandas) \u2014 invalid values are rejected at assignment time:</p> <pre><code>frame = table.to_frame(codelists=codelists)\nprint(frame.schema)\n# {'category': Enum(categories=['Total excluding LULUCF', 'Total including LULUCF']),\n#  'scenario': Enum(categories=['WAM', 'WEM', 'WOM']),\n#  'cyear': Int64, 'cvalue': Float64, ...}\n</code></pre>"},{"location":"guides/schema/#get-an-empty-dataframe-with-correct-types","title":"Get an empty DataFrame with correct types","text":"<p>Returns an empty polars (or pandas) DataFrame whose column names and dtypes match the table schema. Useful for building import data programmatically.</p> <pre><code>table = schema.table(\"Table1a\")\nframe = table.to_frame()\n# shape: (0, 8)  \u2014 zero rows, correct columns and types\nprint(frame.schema)\n# {'category': String, 'scenario': String, 'ry': String,\n#  'cyear': Int64, 'gas': String, 'cvalue': Float64, ...}\n\n# Get all tables at once (optionally with codelists)\nframes = schema.to_frames(codelists=codelists)\n# {\"Table1a\": &lt;empty DataFrame with Enum columns&gt;, ...}\n</code></pre> <p>You can then populate it and pass it straight to <code>import_file()</code>:</p> <pre><code>import polars as pl\n\nempty = schema.table(\"Table1a\").to_frame()\ndata = pl.concat([empty, pl.DataFrame({\n    \"category\": [\"Total including LULUCF\"],\n    \"scenario\": [\"WEM\"],\n    \"ry\": [\"0\"],\n    \"cyear\": [2024],\n    \"gas\": [\"CO2\"],\n    \"cvalue\": [1234.5],\n    \"notation\": [\"NA\"],\n    \"inventorySubmissionYear\": [2024],\n})])\nie.import_file(dataset_id=93953, file=data, table_schema_id=\"68dd41f0...\")\n</code></pre>"},{"location":"guides/schema/#generate-a-template-csv-header","title":"Generate a template CSV header","text":"<pre><code>table = schema.table(\"Table1a\")\nheader = \"|\".join(table.column_names())   # pipe-delimited\nprint(header)\n# category|scenario|ry|cyear|gas|cvalue|notation|inventorySubmissionYear\n</code></pre> <p>!!! warning     Never include a <code>record_id</code> column \u2014 Reportnet assigns it on ingestion.</p>"},{"location":"guides/validation/","title":"Validate a dataset","text":""},{"location":"guides/validation/#trigger-validation","title":"Trigger validation","text":"<pre><code>ie = client.for_dataflow(1619).for_provider(42)\n\nhandle = ie.add_validation_job(dataset_id=93953)\nhandle.wait(poll_interval=10.0, timeout=600.0)\n</code></pre> <p>If another job is already running on the dataset the API returns HTTP 423, which raises <code>DatasetLockedError</code>. Either wait and retry, or catch it explicitly:</p> <pre><code>from reportnet import DatasetLockedError\n\ntry:\n    handle = ie.add_validation_job(dataset_id=93953)\nexcept DatasetLockedError:\n    print(\"Dataset busy \u2014 try again shortly\")\n</code></pre>"},{"location":"guides/validation/#read-validation-results","title":"Read validation results","text":""},{"location":"guides/validation/#bigdata-datasets","title":"BigData datasets","text":"<pre><code>results = ie.list_group_validations_dl(dataset_id=93953)\n# {\"validations\": [...], \"totalRecords\": 12, ...}\n</code></pre>"},{"location":"guides/validation/#citus-datasets","title":"Citus datasets","text":"<pre><code>results = ie.list_group_validations(dataset_id=93953)\n</code></pre>"},{"location":"guides/validation/#download-a-release-snapshot","title":"Download a release snapshot","text":"<p>After data has been officially released/submitted, you can download the validation results for that snapshot as CSV bytes:</p> <pre><code>csv_bytes = ie.download_validation_snapshot(\n    snapshot_id=7,\n    dataset_id=93953,\n)\nwith open(\"validation_snapshot.csv\", \"wb\") as f:\n    f.write(csv_bytes)\n</code></pre>"},{"location":"guides/validation/#release-history","title":"Release history","text":"<pre><code>releases = ie.list_historic_releases(dataset_id=93953)\nfor r in releases:\n    print(r.get(\"releaseDate\"), r.get(\"status\"))\n</code></pre>"}]}