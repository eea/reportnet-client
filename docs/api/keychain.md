# Key storage

Helpers for storing and retrieving API keys in the OS keychain
(macOS Keychain, Windows Credential Manager, libsecret on Linux).

Requires `pip install "reportnet-client[keyring]"`.

::: reportnet.save_key

::: reportnet.get_key

::: reportnet.delete_key
